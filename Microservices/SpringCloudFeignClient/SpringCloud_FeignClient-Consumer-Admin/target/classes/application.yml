#server port
server:
  port: 7777
# service id
spring:
  application:
    name: ADMIN-CONSUMER
# server url
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
# instance id
  instance:
    instance-id: ${spring.application.name}:${random.value}
      

