# this file is used to give input to parent project 
# by default config serve runs on port 8888 and config client will search server with port 8888 only
# if we have changed this port , then we use this file 
# otherwise by default it will go through below keys
spring.cloud.config.uri=http://localhost:8888